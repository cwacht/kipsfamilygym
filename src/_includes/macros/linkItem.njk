{% macro linkItem(url,title,detail,content) %}
  <article class="c-link">
    <h3 class="c-link__title  c-h3">
      {# <a href="{{- url -}}"> #}
        {{ title }}{{ detail }}
      {# </a> #}
    </h3>
    <div class="c-link__summary">{{ content }}</div>
  </article>
{% endmacro %}